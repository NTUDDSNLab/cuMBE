mbe(L,R,P,Q)
    while (P != 空) {

        從P選一個點x出來加入R,產生R'
        
        根據R'算出新的L
        
        檢查Q中有沒有人連全部L,有的話就不是maximal,不用再往下做
        
        如果是maximal
               
                檢查P中有沒有v連全部L,有的話就加進R
                mb_cnt++;
                如果P還有人
                    mbe(L,R,P,Q)
        
        這棵樹結束,把從P選出來的點丟進Q
    }
}

